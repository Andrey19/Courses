<network-security-config>
    <debug-overrides>
        <trust-anchors>
            <!-- CAs for debug only -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>